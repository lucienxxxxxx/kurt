# Dependencies (installed at this workspace root).
node_modules/

# bun
.bun/
bun.lockb

# os / logs
.DS_Store
*.log

# session scratch + agent workspace artifacts (created at runtime in the working dir;
# they hold user/agent-generated data — never commit, never auto-delete).
.kurt/
import/
export/
